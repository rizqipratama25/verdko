server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # PENTING: Biar kalau di-refresh di halaman selain '/' ga 404
        try_files $uri $uri/ /index.html;
    }
}